import React from 'react';
import { Text, View } from 'react-native';

export default function Circle({navigation}) {
    return (
        <View> 
            <Text> circles :) </Text>
        </View>

    );
};