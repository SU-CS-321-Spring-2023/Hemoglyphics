import React from 'react';
import { Text, View } from 'react-native';

export default function Friends({navigation}) {
    return (
        <View> 
            <Text> Friends :) </Text>
        </View>

    );
};
