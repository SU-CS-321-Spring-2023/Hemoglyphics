import React from 'react';
import { Text, View } from 'react-native';

export default function Maps({navigation}) {
    return (
        <View> 
            <Text> maps :) </Text>
        </View>

    );
};